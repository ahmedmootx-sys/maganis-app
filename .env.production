# Production environment. Committed intentionally: placeholders only, no secrets.
# If real endpoints or secrets are ever introduced they live in
# .env.production.local on the deploy machine (gitignored, never committed).
VITE_APP_ENV=production
VITE_APP_NAME=Maganis
VITE_STORAGE_PREFIX=maganis
VITE_LOG_LEVEL=warn
